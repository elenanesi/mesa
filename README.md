# Mesa — meal planner PWA for two

A free, installable, offline-first PWA that plans a week of Mediterranean meals for Elena and Andrea (couple with different calorie/macro targets, shared dinners), generates one household shopping list, and logs what was actually eaten. **Every number is computed, never typed in** — Mifflin-St Jeor for targets, sums over the food DB for nutrition.

**Live:** https://mesa-9y5.pages.dev/app/ (Cloudflare Pages, behind Cloudflare Access — only elenanesi55@gmail.com and angelucci88@gmail.com). Couple sync/catalog backend: https://mesa-sync.elenanesi55.workers.dev (Cloudflare Worker + KV + D1). Legacy public URL https://elenanesi.github.io/mesa/ (to be retired once Elena confirms Access login works → then make the GitHub repo private).

## How agents work on this repo

1. Read `PWA-MVP-plan.md` → **"Ground rules for every agent task"** — they are hard constraints (deterministic numbers; no frameworks/build step; visual parity with the locked design; iPhone Safari ≥44px targets; localStorage-only; verify in a real browser before reporting done).
2. The app is `app/` — plain HTML/CSS/JS, globals, no modules: `js/state.js` (store `mesa.v1`, version 4, migrations), `js/engine.js` (targets + recipeNutrition), `js/planner.js` (deterministic week planner, swaps, re-balance, shopping list), `js/render.js` (all DOM), `js/library.js` (custom foods/recipes + D1 builtin replacement), `js/sync.js` (couple sync + D1 catalog fetch), `js/app.js` (boot/nav), `data/foods.js` + `data/recipes.js` + `data/validate.js` (offline fallback/seed sources; validators must stay `ok:true`).
3. `mesa-prototype.html` is the frozen design reference — never edit it.
4. **Local verification quirk**: the sandbox blocks servers reading this folder — rsync `app/` to the session scratchpad and serve with the `serve_app.py` there (port 8322; `python3 -m http.server` fails; `.claude/launch.json` has a `mesa-app` preview entry). `node` is installed (syntax-check with `node --check`). Never chain reload+assert in one preview eval. **Stale-cache trap (cost us a debugging session)**: serve_app.py must send `Cache-Control: no-store` (it does now) AND Chrome's per-origin memory cache can still serve an old script across normal reloads — if the page behaves like an older build, switch origin (localhost ⇄ 127.0.0.1) or open a fresh tab before suspecting the code.

## Agent handoff lessons from 2026-07-15

- **Deploy is not GitHub.** Several batches were deployed to Cloudflare Pages/Workers before being committed. Always check `git status --short --branch` and `git log --oneline --decorate -5 --all`; if `main...origin/main` shows no commit ahead but files are modified, GitHub does not contain the deployed state yet.
- **Do not silently deploy a partial dirty tree.** This repo may contain expected uncommitted work from other agents. If asked to deploy/push, clarify whether the user wants the whole current local state or only your staged subset. A Pages deploy from a staged copy of `index.html` + `app/` publishes whatever is in the local tree, regardless of Git.
- **D1 changes require three steps.** For schema changes: add a migration under `worker/migrations/`, apply it remotely with Wrangler, then deploy `worker/sync.js`. Afterward, verify with a direct D1 schema/readback command. Client-only Pages deploy is not enough.
- **Installed PWA cache matters.** Run `node tools/build-sw.js` before every deploy — it regenerates `app/sw.js`'s `SHELL_FILES` from disk and stamps a content-hash `CACHE`, so shell/data changes always invalidate old installs. If a phone looks stale after deploy, suspect the service worker/cache before changing logic again.
- **Ingredient edits now use `foodOverrides`.** Built-in ingredient edits should not mutate `data/foods.js` at runtime or be forced into `customFoods`. Use synced household overrides (`foodOverrides`) and keep `applyCustomFoods()`, `librarySectionData()`, merge logic, backup/import, and D1 mirror in sync.
- **Ingredient icons are food data, not renderer logic.** Icon mapping lives on food records (`iconKey`/`iconAsset`) and is mirrored to D1 in `foods.data_json`; do not reintroduce hardcoded ID/name maps in `library.js`. For built-in/global icon metadata changes, deploy Pages AND seed/read back D1 global rows (e.g. verify `oat-milk -> milk`) because a client deploy alone does not rewrite existing D1 catalog rows.
- **Built-in catalog changes: D1 is authoritative at runtime.** `app/data/foods.js` and `app/data/recipes.js` are the bundled offline fallback and seed source; on startup the PWA fetches `GET /library/GLOBAL` from the sync Worker, replaces builtin recipe rows from Cloudflare D1, and falls back to bundled recipes only if the fetch fails/offline times out. Any change to built-in food/recipe fields needs: `node tools/seed-d1.js > /tmp/seed.sql`, then from `worker/`: `npx wrangler d1 execute mesa-library --remote --file=/tmp/seed.sql` (same keychain token env vars as the Pages deploy), then a readback verify (e.g. `SELECT COUNT(json_extract(data_json,'$.role')) FROM recipes WHERE scope='global'`). The script calls the app's own `buildLibraryCatalogPayload()` in a vm context, so seeded rows are byte-identical to a real client push; it also tombstones stale global builtin rows that no longer exist locally. Deploy Pages too for any bundled fallback or static asset change.
- **Re-balance must not touch the past.** When changing planner candidate enumeration, compare each plan day to `todayISO()` and exclude dates before today. Logged/skipped locks are not enough because unlogged Monday/Tuesday meals can still be historical.
- **Do not auto-focus top-level mobile page searches.** iOS/Safari opens the keyboard immediately. Keep auto-focus for explicit picker/search flows where typing is the next action, but not for landing pages such as Library → Ingredients.
- **Barcode/Open Food Facts in Italy:** prefer the localized Italian OFF endpoint before global endpoints; some products visible on the website return richer nutriments from `it.openfoodfacts.org`.
- **Icons: do not repeat the weak generated-icon batch.** Codex produced a poor first pass for ingredient watercolor icons; another agent later fixed/replaced them. Future icon work should preserve the repaired assets, inspect contact sheets visually, and use the `watercolor-ingredient-icons` skill only with actual visual QA before wiring/deploying.
- **Detail hero layout on mobile:** keep the ingredient/recipe detail image slot on the left and tags/meta on the right, even under the narrow mobile media query. Do not stack image-over-tags unless Elena explicitly asks for that layout; it wastes vertical space and made the screenshot look unchanged.
- **Recipe images are separate from ingredient icons.** Shared recipe art lives under `app/assets/recipes/*.png`; `tools/build-sw.js` must include that directory, and `app/sw.js` must be regenerated before deploy. Recipe rows may carry a safe relative `imageUri` such as `assets/recipes/pizza.png`; `render.js` prefers that over `imageKey`. `imageKey` remains the picker/fallback vocabulary, while `recipeImageAssetForRecipe()` and `recipeHeroHtml()` enforce safe paths with default fallback to `assets/recipes/default-recipe.png`. Auto mode checks specific recipe art first, then soup/pasta/salad presentation, then fish/shellfish ingredients, then meal-slot defaults (`breakfast` bowl, `lunch` salad, `dinner` default), so tuna salad stays salad while non-salad fish dishes stay fish. Recipe detail must expose a clear `Change image` action; it opens the recipe editor with the Lead image picker expanded.
- **Library recipe rows should open detail.** The recipe Library row body opens `openRecipe(id, 'libraryRecipes')`; only the small action buttons should favorite/thumbs-down/edit/delete. If a screenshot shows "Edit recipe" after tapping a row, the row tap handler regressed.

## Deploy (both, in this order)

1. **Run `node tools/build-sw.js`** — regenerates `app/sw.js`'s `SHELL_FILES` list from disk and stamps a content-hash `CACHE`, so a forgotten manual bump can no longer ship a stale shell. Commit the regenerated `sw.js`.
2. Commit + `git push origin main` (GitHub repo `elenanesi/mesa`; creds in macOS keychain via `git credential fill`).
3. Cloudflare: stage root `index.html` + `app/` into a temp dir, then
   `CLOUDFLARE_API_TOKEN=$(security find-generic-password -a mesa -s cloudflare-token -w) CLOUDFLARE_ACCOUNT_ID=84766baa4ad939ee067626830dd2f8dc npx wrangler pages deploy . --project-name=mesa --branch=main --commit-dirty=true`
   (token perms: Pages Edit + Access Edit only). Access team domain: lively-unit-4aa5.cloudflareaccess.com.

## Docs

- `PWA-MVP-plan.md` — Phase 1 build plan (DONE) + ground rules (living)
- `install-and-test-guide.md` — user-facing install/test guide
- `MVP-plan.md` — product vision, accuracy architecture, research sources
- `ux-research-notes.md` — UX research behind the design
- `KNOWLEDGE-BASE.md` — how Mesa determines a healthy diet: targets, goal profiles, tag thresholds, sourcing (every number cited to file:line)

---

## STATUS (maintained — integrate & prune when tasks complete)

**Done through 2026-07-13** (sw CACHE mesa-v9): Phase 1 MVP shipped + post-MVP batches — custom food/recipe library with auto-derived tags; editable log (breakfast normal slot, Undo everywhere, Today/Log/Insights parity from logHistory); Week screen starts with the plan (coverage cards on Insights); two-week horizon (This/Next week toggle, per-week shopping, cross-week variety filter, advance swaps survive rollover); swap-anything sheet (best matches + all slot options); merge-only library import; Cloudflare Pages + Access migration done; UI feedback batch — Confirm/Skip/Undo directly on the Today screen cards (same logHistory funnel as Log, verified both directions) + every numeric stepper field also directly typeable with decimals (comma AND dot accepted, inputmode=decimal, ≥16px inputs); **Phase 2 S1/S2 done** — couple sync client + Worker/KV deployed at `mesa-sync.elenanesi55.workers.dev`, Profile "Couple sync" UI enabled, CORS verified for the Pages origin; Cloudflare Access bootstrap links the household code to the allowed login emails so a reinstalled iOS PWA can restore profile/plan/library data after login instead of falling back to defaults.

**Done 2026-07-14** (sw CACHE mesa-v32): Wishlist batch (v16) — Profile section menu bar (sticky chip nav; jumps via direct `scrollTop`, since `scrollIntoView`/smooth no-op inside the absolutely-positioned `.screen` scroller in iOS WebKit); category/tag filter chips in both the Ingredients and Recipes sheets; a computed 1-line "what this week accomplishes" summary on the Week screen; line-SVG section-header icons across Profile/Insights; and `KNOWLEDGE-BASE.md` (nutrition logic, every number cited to file:line). **Couple-sync swap-revert fix** (v17) — a meal slot holds both people's portions but carried one cell-level timestamp, so `mergePlansSection` replaced the whole cell and one partner's edit clobbered the other's swap back to the planned meal; now solo meals stamp + merge per-person (shared dishes still move as one cell), order-independent. **Muscle-goal copy reconciliation** (v18) — "1.6 g protein / kg bodyweight" was neither the mechanism nor the number (protein is the %-of-calories split, ~1.8–2 g/kg at defaults); copy + KB updated to match. **Logged-meal persistence/display fix** (v19) — edited confirmed meals now carry an update timestamp for sync conflicts, and Today/Log cards prefer today's logged entry over a regenerated planned meal. **Coffee counts + occasional foods** (v20) — Log now tracks daily coffee/cappuccino counts, adds robiola/provola/scamorza, and includes occasional gelato/fast-food/brownie recipes without putting them in automatic week planning. **Multi-slot recipes + Library tab** (v21) — recipes can belong to multiple meal slots, planner/swap/library filters respect those slots, the bottom Profile tab is now Library, and Profile opens from the Today header avatar. **Library-add no-reset fix** (v22) — adding/importing recipes or ingredients no longer invalidates the current week plan; legacy customRev signatures are normalized without regenerating. **Recipe search + preference planner + yesterday log** (v23) — recipe library search, favorite/thumbs-down recipe controls synced with the library, planner preference bias/avoidance, a Today/Yesterday log toggle that can confirm or skip yesterday's meals, and the home Mesa coach suggestion removed. **Week/Home display consistency** (v24) — Week rows now use the same logged-plan overlay as the Today cards, so today's row cannot diverge from Home after confirms, swaps, or recovered logged meals; date reads stay side-effect-free. **Nutrition consistency hardening** (v25) — plan views, recipe detail, log rows, consumed totals and Insights now derive from the same live recipe/food nutrition helpers; stale saved plan/log macro fields are fallback only, and recipe detail opens at the planned/logged portion when launched from a meal. **Pinned meals + routines** (v26) — Week rows can pin meals so re-balance leaves them unchanged, and can set soft meal routines (daily, every other day, or same weekday weekly) that seed current/future plans without acting as hard pins. **Recipe action grid** (v27) — Library recipe action buttons now sit in a compact 2x2 grid so recipe titles keep more horizontal room. **Today over-target + ingredient macro edit** (v28) — Today shows negative kcal left when logged food exceeds the target, logged totals use the frozen computed nutrition captured at confirm/add time, and Ingredients now display kcal/protein/carbs/fat/fiber plus edit controls for custom ingredients. **Meal extras + sides** (v29) — meals can carry extra recipe components such as a veggie side, totals/log/shopping include every component, and recipes can now be marked as `Side`. **Today eaten records** (v30) — the Today screen now shows every logged item for the day, including confirmed meals, coffee/cappuccino counts, quick-add snacks, and extra foods. **Editable grouped eaten records** (v31) — Today aggregates identical quick-add foods/drinks (for example 2 cappuccinos), lets those groups be edited or deleted, and meal cards now show protein/carbs/fat. **Logged-meal immutability guard** (v32) — plan regeneration/rebalance now preserves any slot already logged or skipped; adding an extra recipe to an already-logged meal edits only the eaten record, not the underlying plan.

**Done 2026-07-15** (sw CACHE mesa-v33, deployed to Pages + worker): **Meal-extras batch** — recipe screen "Mark as eaten" transitions to ✓ Eaten today/Skipped with Undo (derived from slotLogStatus, re-derived on profile switch); add-to-meal sheet restructured into In this meal / Sides / Full recipes (any slot, cross-slot mains allowed) with per-extra ✕ Remove and 0.5-step portion steppers (plan + logged, couple-sync stamps mirrored from the add path); card button reads ✎ Edit when a meal has extras; recipe screen shows an "In this meal" strip (base + extras + meal total + Manage) so card totals reconcile with per-recipe numbers. **Security batch (code review)** — worker `/bootstrap` now verifies the Cloudflare Access JWT (team certs, iss/exp, aud vs the ACCESS_AUD worker secret) and takes the email from the verified token only, plus 10/h/IP rate limiting (NOTE: household code from before this fix should be treated as leaked — rotate by leave/create/join on both phones); stored-XSS sweep across render/library/planner/state (recipe steps, food/recipe names, jsAttr hardened against attribute breakout). **Consistency batch** — meal-routine edits stamp merge timestamps (no more sync reverts); Insights/re-balance/week-summary include meal extras; adding an extra to a logged meal updates the plan entry too (supersedes v32's log-only behavior — swap corrections and shopping stay consistent); sw navigations no longer cache.put into the versioned cache (no HTML/JS skew). **Library sync dedupe** (the "Frittata di pasta ×200 (imported)" bug) — custom entries carry per-entry `u` stamps; couple sync uses a convergent newer-wins mergeLibrarySection instead of the file-import merge (which ratcheted duplicates every sync round); deletions tombstone in deletedRecipes/deletedFoods (epoch-ms) so they propagate; a one-shot boot cleanup collapses existing " (imported)" duplicates, tombstones them, and remaps plan/log/pref references (🧹 toast on first launch).

**Done 2026-07-15** (sw CACHE mesa-v34): **Barcode ingredient import** — Log/Add and Library now expose a Scan barcode flow. The PWA opens the camera with `getUserMedia()`, uses native `BarcodeDetector` when available, keeps a manual barcode fallback for browsers without native decoding, looks up products in Open Food Facts API v3.6, previews the product/ingredients/macros, and saves it as a normal `customFoods` ingredient with barcode, brand, quantity, label kcal, ingredient text, Nutri-Score/NOVA, source URL, image URL, allergens/traces/additives metadata, and Mesa-computed kcal from label macros. The imported ingredient follows the same sync/export/library path as hand-added ingredients.

**Done 2026-07-15** (sw CACHE mesa-v35): **iPhone barcode scanner fix** — widened the camera frame for horizontal EAN/UPC barcodes and vendored ZXing (`app/vendor/zxing-browser.min.js`) as the fallback scanner when iOS Safari exposes camera access but not native `BarcodeDetector`; the manual lookup error now clearly says a missing product is missing from Open Food Facts, not necessarily from commercial apps such as Yuka.

**Done 2026-07-15** (sw CACHE mesa-v36): **Open Food Facts localized lookup fix** — barcode import now tries `it.openfoodfacts.org/api/v3` before the global endpoints because some Italy-visible products (e.g. Nesquik Conchigliette `3387390331660`) return product identity from the global API but empty `nutriments`, while the localized Italian endpoint returns full nutrition.

**Done 2026-07-15** (sw CACHE mesa-v37): **Recipe serving log fix** — recipe-detail serving steppers now write back to the actual plan/log context before confirming, so if Elena changes a light meal from the planner's 1.5x suggestion to the 1x she actually ate, the log freezes 1x as eaten instead of restoring the planned target portion.

**Done 2026-07-15** (sw CACHE mesa-v38): **Plain ingredients in meals** — the meal add/edit sheet now supports plain food extras as well as recipe extras, so a logged/planned meal can be "latte e cereali + banana" without creating a full recipe. Meal components can now be `{recipeId, portion}` or `{foodId, grams}`; nutrition, Today/Log titles, and shopping aggregation all count both deterministically.

**Done 2026-07-15** (sw CACHE mesa-v39): **Today swap action** — Today meal cards now show a swap button alongside eaten/add/skip actions, reusing the same swap sheet and plan-safe mutation path as Log.

**Done 2026-07-15** (sw CACHE mesa-v40): **Elena pesto ingredient** — added basil, pecorino romano, and `Pesto Elena` as built-in ingredients. Pesto uses Elena's batch formula (50g basil, 70g parmesan, 30g pecorino, 15g almonds, assumed 20ml olive oil "a sentimento") and stores computed per-100g nutrition.

**Done 2026-07-15** (sw CACHE mesa-v41): **Green beans ingredient** — added `Green beans, cooked` as a built-in produce ingredient for direct meal additions and recipe building.

**Done 2026-07-15** (sw CACHE mesa-v42): **Home macro calories redesign** — replaced the hard-to-read stacked "Calories by macro" strip with three compact rows showing each macro's target kcal, grams, and percentage.

**Done 2026-07-15** (sw CACHE mesa-v43): **Favorite recipes first + Elena recipe wishlist** — recipes marked with a heart now sort to the top in the recipe library, recipe search, swap search, and add-to-meal recipe picker. Added Elena's breakfast/lunch/dinner/side wishlist recipes as built-ins, plus the missing staple ingredients needed to compute them from the food DB.

**Done 2026-07-15** (sw CACHE mesa-v44): **Library as real pages** — moved Library before Insights in the bottom nav and replaced the Library bottom-sheet hub with dedicated Library, Ingredients, Recipes, and Library scanner screens. Ingredient/recipe create-edit flows now repaint those pages instead of overlaying the app.

**Done 2026-07-15** (sw CACHE mesa-v45): **Compact library filters + D1 catalog groundwork** — Ingredients/Recipes keep search visible and collapse category/tag filters behind a compact Filters control with active-chip summaries. Added Cloudflare D1 schema and Worker/client catalog-mirror code so built-in and custom foods/recipes can live together in inspectable SQL tables; activation is blocked until a D1-capable Cloudflare token/database id is available.

**Done 2026-07-15** (sw CACHE mesa-v47): **Season-aware library and planning** — added `season` as a recipe/ingredient dimension (`evergreen`, `winter/autumn`, `spring/summer`) with Library filters, custom ingredient/recipe form controls, conservative seasonal tagging for obvious produce and recipes, D1 `season` columns/indexes, and automatic planning/re-balance eligibility limited to the current season plus evergreen items.

**Done 2026-07-15** (sw CACHE mesa-v48): **Ingredient edit parity + future-only re-balance** — Ingredients can now be edited from the dedicated Library ingredients page, including built-in ingredients via synced household overrides; edited ingredients show an `edited` badge and can be reset. Week re-balance now ignores dates before today, so a Wednesday re-balance cannot propose changes to Monday or Tuesday.

**Done 2026-07-15** (sw CACHE mesa-v49): **DB-driven watercolor ingredient icons** — moved ingredient icon mapping into `FOODS`/D1 `data_json` via `iconKey`/`iconAsset`, replaced the hardcoded `library.js` mapping, added a generic watercolor default food icon cached in PWA `localStorage`, and kept exact/reused icon keys for the current watercolor set.

**Done 2026-07-15** (sw CACHE mesa-v50): **Ingredients search keyboard fix** — opening Library → Ingredients no longer auto-focuses the search input, so mobile Safari does not open the keyboard until the user taps Search.

**Done 2026-07-15** (sw CACHE mesa-v51): **Produce watercolor icon completion** — added custom watercolor PNGs for the remaining built-in produce ingredients that still fell back to the generic icon (mixed berries, spinach, red onion, lemon juice, green beans, peaches, pears, mushrooms, pumpkin/squash, escarole/endive, romaine lettuce, garlic, potatoes, oranges, rocket/arugula, ginger, sweet potato), wired them via `FOODS` `iconKey`, and mirrored the metadata to D1 so Library/Ingredients reflects the same DB-driven mapping.

**Done 2026-07-15** (sw CACHE mesa-v52): **Protein pancakes + bakery watercolor icons** — added `Pancakes proteici` as a computed high-protein breakfast recipe, with explicit whey protein powder and chocolate-hazelnut spread ingredients so nutrition remains ingredient-derived. Added watercolor icons for all Bakery foods (rye bread, whole-wheat bread, white bread, pizza bianca/focaccia, pasta filo, fast-food beef burger, brownie), wired by `iconKey`, and mirrored the food/recipe catalog to D1.

**Done 2026-07-15** (sw CACHE mesa-v53): **Watercolor app icon** — replaced the flat bowl app icon with a minimal watercolor cornucopia across PWA/icon PNG sizes, SVG fallbacks, and the Worker-served iOS `apple-touch-icon`; remember that iPhone icon updates need both the Pages HTML cache-buster and the Worker `/assets/icon-180.png` base64 updated/deployed.

**Done 2026-07-15** (sw CACHE mesa-v54): **Reinstall profile restore hardening** — Cloudflare Access bootstrap now creates a household code automatically for allowed users if none exists yet, so profile/plan/log data starts syncing without requiring the user to manually create Couple sync first. Reinstall still cannot recover data that only ever lived in deleted localStorage; after this deploy, opening the current install once is enough to seed the cloud backup for future reinstalls.

**Done 2026-07-15** (sw CACHE mesa-v55): **Log sync tombstone fix** — log tombstones are now timestamped and separated from library tombstones. Re-confirming a meal slot (for example breakfast) clears stale `plan:<slot>` / `skip:<slot>` tombstones, while skip/delete writes a newer tombstone, so confirmed meals, skipped meals, and quick-add foods converge correctly across reinstall/device sync instead of a stale slot tombstone suppressing a newly logged breakfast.

**Done 2026-07-15** (sw CACHE mesa-v56): **Manual swap/correction hardening** — swap search now survives malformed/stale recipe records instead of breaking the bottom sheet, and every swap opener attaches the same non-inline search handler. Manual swaps use the actual meal cell's shared/solo state (not the slot default), update already-confirmed log entries in place, and can target the selected Log date (including yesterday); automatic generation/re-balance still preserves past or logged meals, but explicit user corrections remain allowed.

**Done 2026-07-15** (sw CACHE mesa-v57): **Swap persistence fix** — manual swaps and serving edits now call `markWeekPlanEdited(plan)` immediately after mutating the plan. This refreshes the week signature before any render/sync path can call `ensureWeekPlan()` again, preventing the freshly swapped meal from being treated as a stale/generated plan and restored to the old default.

**Done 2026-07-17** (sw CACHE mesa-023ab0ebeb35 — now content-hashed, see below): **Code-health batch (5 refactors, net −256 app lines, zero behavior change)** — (1) `tools/check.js`: zero-dep node regression harness (137 tests; loads app files via `vm` like script tags with a `MESA_TEST_TODAY` date hook in `todayISO()`), covering validateData, nutrition determinism, the library-merge duplication ratchet, log tombstones (v55 semantics), plan-cell merges, planner determinism, meal-extras semantics, escaper hostility payloads, and sw.js shell drift — run it before every deploy. (2) `tools/build-sw.js`: `app/sw.js` is now MACHINE-GENERATED (SHELL_FILES from disk + content-hash CACHE) — never hand-edit or manually bump again. (3) Meal-extras dedup: the six planner extras functions are thin wrappers over `mutateMealExtras()`/`findLastExtraIndex()` (couple-sync stamp + shared-mirror semantics live in ONE place now); render.js logged-meal extras share `loggedMealComponents()`/`commitLoggedMealComponents()`. (4) XSS hardening: canonical `escapeHtml`/`htmlAttr`/`jsAttr` defined once in state.js (guard-tested); all user-content lists (library rows, swap/search results, add-meal sheet, quick-add, full Week list) converted from inline-onclick string interpolation to `data-*` attributes + delegated listeners — user strings never enter a JS-string context. (5) Legacy `RECIPES` compat view deleted (render reads RECIPES_DB via `recipeDisplayIngredients()`/`recipeDisplayPills()`; a check.js guard forbids bare `RECIPES` references), and state.js's log-history API moved verbatim to new `app/js/log.js`. Full-browser integration pass: fresh boot zero-network, every screen exercised incl. hostile-named library entries rendering inert, mobile viewport clean. Known pre-existing (not regressions): Library ingredient-count label goes stale while typing a search; Library recipe rows have no tap-to-detail affordance.

**Done 2026-07-17 (2)** (sw CACHE mesa-a3857fafc34e): **Features batch B1–B5** (plan: `FEATURES-2026-07-plan.md`; decisions Q1–Q4 recorded there) — **(B1) Real goal toggles**: Profile goals are per-person rendered from `GOAL_DEFS` (Andrea no longer sees Elena's fat-loss option) and persisted as `goals` booleans; `goalAdj/goalName/goalTag` are now DERIVED in engine.js (fatLoss −325 / muscleGain +60 / else maintenance), so unchecking "Gentle fat loss" immediately moves the daily target (1,820 ⇄ 2,150) and re-targets future plan days via the signature path; whyText skin/thyroid clauses follow the toggles. **(B5) Catch-up logging from Week**: current-week rows with `date <= today` swap pin/routine for a ◯/✓/∅ log button opening an Eaten/Skipped/Undo mini-sheet; backdated entries freeze combined macros with `t:null` + fresh `u`. **(B4) Nutrient summaries**: expanded days show a `P·C·F·fiber·free-sugars` line and both weeks get an avg/day card (fiber vs 25g, free sugars vs the 6%-kcal target — constants reused, not re-typed; omega-3/satFat chips share Insights' renderer); current week sums the logged overlay so Week/Today/Insights cannot disagree. **(B3) Week-level sides**: the add-meal sheet moved to an explicit `{weekStartDate, dayIndex, slot, person}` context (Today/recipe-screen callers unchanged via adapter) and every week row (both weeks) gained a ＋ extras button. **(B2) Meal roles + composition**: every recipe carries `role: full|main|side` (67/14/9; validate.js enforces presence, warns on kcal-plausibility; library forms expose role + breakfast-pairing controls; fields ride sync/D1 data_json automatically); the planner now composes `main + side` units for lunch/dinner (top-4 side pruning, side portions 0.5/1, sides skip the style filter, equal scoring vs full meals per Q1) and pairs light breakfasts with whitelist bakery/fruit foods (piece/30g steps, Q2 list), emitted as ordinary meal-extras so titles/logging/shopping/sync all work unchanged; snack never composes; graceful full-only fallback on thin pools; re-balance side pool is role-based. Composed density observed: ~14/28 lunches+dinners, ~8/14 breakfasts. NOTE for future recipe work: lunch `role:'main'` pool is thin (2 in balanced style) — add main-course lunch recipes to enrich composition variety. Harness now **221 tests**; cross-batch integration pass (8 intersection flows incl. catch-up-logging a swapped composed meal) green, zero console errors, zero network calls.

**Done 2026-07-18** (sw CACHE mesa-616ef92e31ea, deployed to Cloudflare Pages Production): **Detail images + recipe art** — ingredient detail now uses a compact `.detail-hero` instead of the old wide blank image card, with mobile explicitly preserving image-left/tags-right. Recipe detail reuses that shell and renders safe recipe images from `app/assets/recipes/` with category inference (default, salad, cooked vegetables, meat main, fish main, breakfast bowl) and emoji fallback if an image fails. Auto image inference now gives fish/shellfish ingredient recipes the fish-main/salmon art before applying the breakfast/lunch/dinner defaults. The service-worker builder now precaches recipe images alongside ingredient icons. Library recipe rows are tappable to the recipe detail page (`Back` returns to Recipes), while action buttons remain separate. Follow-up: recipe create/edit now has an ingredient-style image picker over the available recipe image set, persists `imageKey` on custom recipes and built-in recipe overrides, and can return to Auto mode. Harness now **405 tests**, including recipe image path safety/SW drift, picker persistence, fish-ingredient inference, and row-tap regression guards.

**Done 2026-07-18 (2)** (sw CACHE mesa-4cbb0adf2c03): **Recipe catalog cleanup + specific recipe art** — built-in recipe display titles are English-only now, with stable recipe IDs preserved for sync/plans; the true duplicate `white-bean-tuna-salad` was removed in favor of the canonical `tuna-white-bean-salad`; ambiguous titles were corrected where ingredients disagreed (`filetto-manzo` now says lean beef patty, `salmone-o-sogliola` now says salmon). Added five 512px watercolor recipe assets (`ramen`, `butter-chicken`, `chinese-dinner`, `fast-food-menu`, `dessert-sweets`) and wired them through the safe recipe image picker/inference path; fast-food art is intentionally generic/no-logo. Harness now covers catalog cleanup, new image keys, and recipe image inference.

**Done 2026-07-19** (sw CACHE mesa-e2c4f6f64aac, branch `claude/recipe-options`, built on task D1's e7b44dc recipe-options engine): **Batch D2 — sauce role + catalog additions** — new role `'sauce'` (label "Sauce & condiment"): excluded from standalone slot planning (never a real meal slot) and from side auto-composition (`sidePoolFor` only matches role `'side'`), offered instead in the add-meal sheet's new "Sauces" section (`mealRecipeOptions()`/`openAddMealSheetForContext`, same delegated-row pattern as Sides); `data/validate.js` gained a `sauce` WARNING kcal band (40-250/serving) and extended the side-role tagging-slip warning to cover it. Added `tomato-basil-sauce` and `yogurt-herb-sauce`. New ingredient `sea-bass-fillet` (USDA FDC 175142, sea bass mixed species raw; kcal recomputed via the file's 4/4/9-from-sourced-macros policy, 92 not the source's raw 97). `french-toast-fruit-maple` gained `optionGroups.fruit` (mixed berries default / banana / peach) with the base list unchanged in nutrition (asserted byte-for-byte against pre-edit values in the harness). Two new optionGroups recipes: `baked-fish` (role `main`, lunch/dinner; fish: salmon/sea bass/sole/cod) and `pasta` (role `full`, lunch/dinner; condiment: tomato & basil/Pesto Elena/tuna & olives/courgette & ricotta). Three new `main`-role recipes fatten the thin lunch-main pool (5→9 overall, 3→7 in `balanced` style): `lemon-herb-chicken-breast`, `turkey-cutlets-sage`, `white-bean-rosemary-mash`. Fix: `butter-chicken.season` corrected to `'winter/autumn'` (was unset/evergreen); the "Burrito vegetariano" Italian title was already fixed pre-batch, confirmed clean app-wide. `validateData()` stays `ok:true` with **zero** errors/warnings against the full catalog. Harness now **519 tests** (was 457), including sauce-exclusion end-to-end (two-week generated plan scan), per-choice kcal-band coverage for every new optionGroups choice, and the french-toast no-opts nutrition-preservation regression.

**Done 2026-07-19 (2)** (sw CACHE mesa-0ea8b0388971): **Recipe lead-image affordance** — recipe detail now has a visible `Change image` action inside the hero, which opens the recipe editor with the Lead image picker already expanded. The recipe editor label is now `Lead image`, with a larger preview and explicit `Choose lead image` action so it matches the ingredient icon picker mentally. Harness now **533 tests**, including the detail-to-picker and normal-edit affordance regression guards.

**Done 2026-07-19 (3)** (sw CACHE mesa-80d62a7c94d3): **Recipe image expansion** — replaced `chinese-dinner` art with a Chinese takeout container; added 11 new 512px watercolor recipe assets (`soup`, `pasta`, `onigiri`, `french-toast`, `pancakes`, `boiled-chicken-broth`, `burrito`, `citrus-roast-turkey`, `club-sandwich`, `ice-cream`, `shakshuka`). Deleted the built-in `pasta-pomodorini-funghi-broccoli` recipe. Recipe image Auto now prioritizes soup/pasta/salad presentation before fish ingredients, with `baked-fish` pinned to `fish-main` and tuna salads staying on `salad`. Harness now **543 tests**.

**Done 2026-07-19 (4)** (sw CACHE mesa-3ce5226e93e2, deployed to Cloudflare Pages Production): **D1-first recipe catalog + approved ad hoc recipe art** — built-in recipes now load from Cloudflare D1 at startup through the sync Worker `GET /library/GLOBAL`, then fall back to bundled `app/data/recipes.js` only when offline or the fetch times out. `library.js` replaces global builtin recipe rows before custom recipes are applied, so production recipes and their image URIs come from the database instead of the shipped fallback. Removed/tombstoned `chicken-tacos-gyros`, `polpette-melanzane`, `prawn-courgette-tomato`, and `beef-courgette-ragu` from every catalog path. Kept the earlier approved recipe assets only, added 512px watercolor art for `pizza`, and added `imageUri` support/validation so D1 rows can point directly at `assets/recipes/<name>.png`. Remote D1 readback confirmed 96 builtin recipes, including `pizza` with `imageUri: assets/recipes/pizza.png`; harness now **657 tests**.

**Done 2026-07-19 (5)** (sw CACHE mesa-1e33263118d2, deployed to Cloudflare Pages Production + Worker): **Code-health/security batch (5 fixes, each pushed + deployed separately)** — (1) **Global catalog writes are now admin-only.** `worker/sync.js`'s `/library/:code` POST had NO authentication and `catalogScopeForRow()` mapped any `source:'builtin'` row to the `global` scope, so anyone who knew the public Worker URL could rewrite the built-in recipes/nutrition that D1 now serves as authoritative to both phones — confirmed live against production before the fix. `isHttpWritableScope()` rejects `scope='global'` over HTTP; globals are written ONLY by `tools/seed-d1.js` → `wrangler d1 execute` (direct SQL, never touches the Worker), which is already the documented workflow. `mirrorLibraryCatalogToD1()` correspondingly stops sending builtin rows, and signs/POSTs the same filtered payload so the skip-if-unchanged short-circuit stays honest. This also closes a second hole: builtin rows mirrored with `updatedAt: 0` and upserted unconditionally, so a stale phone could revert the whole catalog for both users. NOTE: `buildLibraryCatalogPayload()` itself is unchanged — `seed-d1.js` calls it in a vm and needs the builtin rows; the filter is at the mirror call site only. (2) **A degraded D1 response no longer shrinks the catalog.** `replaceBuiltinRecipesFromCatalogRows()` wiped the entire bundled catalog and installed whatever arrived, guarded only by "≥1 row", validating just `title && slot` — so a truncated/partially-seeded response silently cut 96 recipes to however many came back, permanently (the fallback was already discarded), leaving the planner in its "empty candidate pool" path with no visible cause. Added a 50%-of-`BUILTIN_RECIPE_COUNT` sanity floor (baseline 96 → floor 48; captured once at parse time so it can't ratchet down across calls) that rejects the whole payload and keeps the bundled fallback, plus stricter per-row validation (non-empty title, slot in `VALID_SLOTS`, non-empty ingredients) with dropped ids logged. (3) **Storage failures are now visible.** `persist()` — the only path that writes state — swallowed quota/private-mode errors with a `console.warn` nobody reads on a phone, so a full disk meant a day of logged meals vanishing on reload with zero signal. New optional `onMesaPersistFailed(err)` hook fires on the healthy→unhealthy *transition* (not once-ever, not per write), implemented in render.js as a toast; follows the existing `onMesaBeforePersist`/`onMesaAfterPersist` pattern so state.js keeps zero hard dependency on later files. The hook call is deliberately defensive — `toast()` dereferences `#toast` with no null guard, so the naive version threw from inside `persist()`'s catch and propagated out, converting a graceful degradation into a hard crash; the unhealthy flag is flipped *before* notifying and the call is wrapped. (4) **Removed the base64 icon cache** that duplicated `assets/ingredients/default-food.png` into localStorage — the SW already precaches it via `SHELL_FILES`, so this was ~33%-inflated duplication competing for the same iOS quota `persist()` needs, and it was the app's only unconditional load-time fetch (both `check.js` and `seed-d1.js` had to pre-seed a fake key to suppress it). A one-shot `dropLegacyDefaultFoodIconCache()` reclaims the key on existing installs, since deleting only the writer would strand the blob forever. (5) **Dead code + a latent NaN**: deleted `isUnitPinned`/`toggleRoutineOccurrencePinned`/`setRoutineOccurrencesPinned`/`todayRebalanceScore`/`rebalanceValueAfter` (zero references anywhere incl. inline `onclick=` handlers), and `recipeNutrition()` stopped dividing `totals.sugarQuality` — a string — by servings into `NaN` (`typeof === 'number'` guard, not a `NUTRIENT_KEYS` whitelist, since `goodFat` is numeric but absent from that list). Harness now **692 tests** (was 657); every fix was verified failing-without-fix by reverting its source file alone and confirming exactly the intended assertions break. **Agent-coordination lesson (2026-07-19):** two agents worked this tree concurrently and one committed with `add -A`, sweeping the other's uncommitted security fix into an unrelated commit and deploying it unreviewed — the README's "do not silently deploy a partial dirty tree" warning, realized from the other direction. Commit explicit paths, never `-a`/`add -A`, when another agent may be live in the tree.

**Done 2026-07-20** (sw CACHE mesa-81cbf75682dd, deployed to Cloudflare Pages Production): **Pantry/Fridge feature (P1–P3, plan: `PANTRY-plan.md`, decisions Q1–Q3 recorded there)** — the household can record what it has at home; logging meals updates what's left; the shopping list shows only what they don't and won't have. **Architecture — derive, don't mutate (the load-bearing decision):** the pantry stores only a BASELINE per food (`pantry: foodId -> {qty, setAt, u}`, household-level), and what's left is COMPUTED as `baseline − consumedSince(setAt)` over `logHistory`. A mutating pantry ("on log, subtract") was explicitly rejected: it double-counts under couple sync (both phones apply the same delta before merging), needs compensating add-backs for undo/skip/delete consistent with log tombstones, and misapplies backdated catch-up logging (task B5). Deriving from `logHistory` — already convergent, tombstoned and sync-merged — gets all of that for free. **(P1)** Extracted `foodQuantitiesForComponents()` from `computeShoppingList`, so meal→`{foodId, qty}` decomposition (batch yield, `optionGroups` via `recipeEffectiveIngredients`, piece-vs-gram units, meal extras) is ONE helper with two callers and the shopping list and pantry cannot disagree about what a meal contains; `computeShoppingList` builds one flat order-preserving component list so float addition order is unchanged, verified byte-identical across a pinned two-week plan (44/43 rows) BEFORE any pantry code landed. Each totals row gained `foodIds[]` so P3 subtracts by stable id without re-keying the name-keyed `checkedShopByWeek` (no migration). `'pantry'` is a MERGED sync section (not LWW — LWW loses a partner's concurrent edit), reusing the existing `mergeEntryMap` from the "(imported)" duplication-ratchet fix; a delete is a `qty:0` entry with a fresh `u`, so `isValidPantryEntry` accepts `qty >= 0` (the plan said `> 0` and was wrong — tombstones must survive `loadState`). **No Worker or D1 change: `worker/sync.js` dispatches sections via `Object.keys(parsed.sections)` with no per-name logic.** **(P2)** New `app/js/pantry.js` (loaded between planner.js and render.js): `pantryConsumedSince()`/`pantryRemaining()`, each food consuming from its OWN `setAt`. **Consumption is filtered on WHEN THE FOOD WAS EATEN (entry date + `t`), never on the entry's `u` stamp** — `u` is the sync/conflict stamp and `upsertLogEntry` re-stamps it on every edit, so a `u` filter wrongly deducted a catch-up-logged meal eaten BEFORE the baseline (the baseline is a physical count that already reflected it) and dragged a corrected weeks-old meal's whole ingredient list into today's pantry. `t:null`/absent resolves to END of day, so a same-day ambiguity counts the meal — under-counting consumption is the worse error (the pantry would claim food that isn't there and it would never get bought). No tombstone filtering needed: `mergeLogSection` drops tombstoned entries at ingest and the local paths splice them out, so a deleted meal is absent from the walk on both phones. Library → Pantry page lists each food with icon, remaining qty and an age hint, with decrease/remove directly on the row; **every edit funnels through ONE mutator, `setPantryRemaining()`, writing qty + fresh `setAt` + fresh `u` atomically** — a stale `setAt` re-subtracts the pre-edit consumption and shows LESS than the user just typed. **(P3)** Current-week list excludes meals already logged/skipped (Q1); pantry subtracted with fully-covered rows moving to an "Already at home" summary and partial rows annotated (a silently vanishing item is indistinguishable from a bug); next-week list subtracts only PROJECTED leftovers (`pantryRemaining()` minus this week's still-outstanding demand) so an item fully eaten by Thursday doesn't wrongly reduce next week; "Add ticked items to pantry" (Q2) stocks ticked rows on top of existing stock through the re-baselining mutator, while ticking alone stocks nothing. Also fixed a side effect P3 would have introduced: `slotLogStatus()` reads through `getDayLog()`, which lazily CREATES an empty `logHistory` record for any date asked about, and `pruneLogHistory()` drops records only by age, never by emptiness — so every shopping-list view would have left 7 empty day records persisted and synced for 60 days; `slotLoggedReadOnly()` checks for the record first (a date with no record has nothing logged by definition). Harness now **781 tests** (was 692); every phase verified failing-without-fix by reverting its source file alone.

**In progress / next:**
- **Watercolor icons (T2/T4, "Elena generates, agent wires")**: for future non-produce gaps, continue replacing fallback ingredients by adding PNGs under `app/assets/ingredients/<iconKey>.png`, setting `iconKey` on the relevant `FOODS` records, bumping `app/sw.js`, and seeding/readback-checking D1; no hardcoded icon maps in `library.js`.
- **Recipe variety gaps for better planning**: add more soups/minestrone, warm grain/legume bowls, fish variety (sea bass, mackerel, tuna steak, seafood pasta), vegetarian complete-protein mains (dal, bean chili, tofu curry/tagine), simple pasta/rice staples (tomato-tuna pasta, risotto, rice and peas), sheet-pan meals, wraps/piadine beyond chicken, and real-life options like pizza night, sushi/poke, and aperitivo-style meals.
- **Next generic recipe images**: highest leverage after this batch would be rice/grain bowl, egg main, vegetarian main, sheet-pan dinner, sushi/poke, and a less-specific curry image if butter chicken starts feeling too narrow. For named/ad hoc recipe art, add only after Elena approves the recipe IDs; keep ingredients such as salmon/yogurt as ingredient icons, not recipe images.
- Awaiting Elena: confirm Access login + couple sync on the real phones at https://mesa-9y5.pages.dev/app/ → then make GitHub repo private + retire legacy URL in docs.
- Next Phase 2 item: LLM endpoint (needs an Anthropic API key from Elena, proxied by a Worker — key never ships in the app).
